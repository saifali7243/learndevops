apiVersion: 1

contactPoints:
  - orgId: 1
    name: telegram-alert
    receivers:
      - uid: telegram1
        type: telegram
        settings:
          bottoken: ${TELEGRAM_BOT_TOKEN}
          chatid: ${TELEGRAM_CHAT_ID}
          parse_mode: Markdown          
          message: |
            🚨 ALERT: {{ .CommonAnnotations.summary }}
            
            🔥 Status: {{ .Status }}
            📈 Value: {{ .Values }}
            🏷 Labels: {{ .CommonLabels }}
